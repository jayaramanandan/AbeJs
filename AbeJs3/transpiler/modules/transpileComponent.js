class App {
  componentsJs = {
    staticJs: "",
    componentCount: 0,
    classes: {},
    instances: {},
  };

  constructor(file, folder) {
    this.transpileComponent(rootFilePath);
  }

  async transpileComponent(folder, file) {}
}
